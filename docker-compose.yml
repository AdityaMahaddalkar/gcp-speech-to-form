version: "3"

services:
  server:
    build:
      context: ./stof-backend
      dockerfile: Dockerfile
    ports:
      - 8000:8000
  ui:
    build:
      context: ./stof-frontend
      dockerfile: Dockerfile
    environment:
      NODE_ENV: prod
    ports:
      - 80:80
